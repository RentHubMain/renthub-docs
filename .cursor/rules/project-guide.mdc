---
description: RentHub文档站（Docusaurus）全局规范
alwaysApply: true
---

# RentHub文档站 — 项目规范

## 项目性质

本仓库 **`renthub-docs`** 是 **文档项目**：使用 **Docusaurus 3** 构建静态站点，即 **RentHub文档站**（`docs.renthub.cloud`），面向团队内外读者提供可检索的 **综合文档与知识库**（产品、设计、开发工程、法律协议等专题；可持续扩展）。

- **本仓库仅包含文档站源码**（配置、`docs/`、`src/pages/`、`src/theme/` 等），**不包含**小程序、云函数、管理后台、官网等业务实现代码。
- **RentHub（租汇）产品线**（轻资产服务平台：物品与工业设备 **B2B/C2C 租赁**，当前以 **C2C** 为主；基于微信小程序并含 Web 管理后台）在工程上通常采用 **Monorepo**；文档叙事统一使用 **租户（承租方，lessee）**、**商户（出租方，lessor）**（C2C 个人卖家亦称商户）。常见模块划分用于文档叙述与读者上下文理解，例如：
  - 小程序前端（WXML + WXSS + JS）
  - 云函数（user / asset / order / help / message / admin 等）
  - React + TypeScript Web 管理后台
  - 官方网站  

  上述目录**不**在本仓库中出现；若文档需要指向业务仓库，使用对外链接或团队内约定路径，勿写成本仓库内的相对目录。

**站点**：生产环境 `url` 为 `https://docs.renthub.cloud`，`baseUrl` 为 `/`（见 `docusaurus.config.ts`）。自定义域名由 `static/CNAME` 声明。

---

## 技术栈

- **静态站点生成器**：Docusaurus 3（React + MDX），`preset-classic`，**未启用 blog**
- **语言**：TypeScript（配置与主题组件）+ Markdown（文档正文）
- **国际化**：`i18n.defaultLocale` 为 `zh-Hans`（当前仅该 locale）
- **部署**：GitHub Actions → GitHub Pages（见 `.github/workflows/deploy-pages.yml`）
- **主题**：Docusaurus classic + Infima CSS（主题色 `#3d7c47` / `#7cb342`）
- **依赖管理**：npm（`package.json`）；`overrides` 将 `webpack` 固定为 `5.95.0`（与 `webpackbar@6` / Node 高版本兼容性相关，见文末说明）

---

## 目录结构规范

```
docusaurus.config.ts     # 站点 url/baseUrl、navbar、footer、i18n、staticDirectories
sidebars.ts              # 文档侧栏（docsSidebar）
package.json             # 依赖与 scripts
tsconfig.json            # TypeScript 配置
babel.config.js          # Babel 配置

src/
  css/custom.css         # 全站样式（Infima 变量覆盖，品牌色）
  pages/index.tsx        # 站点首页（Hero、新手路线、板块卡片等）
  pages/index.module.css # 首页样式模块
  theme/DocRoot/Layout/  # swizzle 的文档页根布局（侧栏宽度、展开按钮、主内容区等）
    index.tsx
    styles.module.css
    Main/
    Sidebar/
      ExpandButton/

static/                  # 构建时复制到站点根目录
  .nojekyll              # 禁止 GitHub Pages 使用 Jekyll
  CNAME                  # 自定义域名（如 docs.renthub.cloud）

assets/
  images/                # 图片资源（banner、icon 等；在 staticDirectories 中注册）

docs/                    # 所有文档内容
  vibe-coding/
    _category_.json
    index.md
    cursor-guide.md
    cursor-concepts.md
  project-mgmt/
    _category_.json
    index.md
    git-basics.md
    git-workflow.md
  <future-topic>/        # 新板块按需新增
    _category_.json
    index.md
```

- 每个板块下用 `index.md` 作为入口索引
- 子话题用独立 `.md` 文件，文件名使用 **kebab-case**（全小写、连字符分隔）
- 图片等静态资源放在 `assets/images/`（已在 `docusaurus.config.ts` 的 `staticDirectories` 中注册）

---

## 文件命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| 板块入口 | `index.md` | `docs/overview/index.md` |
| 板块元数据 | `_category_.json` | `docs/overview/_category_.json` |
| 普通文档 | `kebab-case.md` | `cloud-functions.md` |
| 板块目录 | `kebab-case/` | `dev-notes/` |

- 名称应简洁且自解释，见名知义
- 避免缩写（除非约定俗成，如 `db`、`api`）

---

## Markdown 文档规范

### Front Matter

每个 `docs/` 下的 `.md` 文件必须包含 YAML front matter：

```yaml
---
title: 页面标题
---
```

板块入口 `index.md` 无需指定 `slug`，Docusaurus 会根据文件路径和 `_category_.json` 自动处理路由。

子文档也无需指定 `slug`，Docusaurus 根据文件路径自动生成 URL。

### 写作规范

- **语言**：中文为主，技术术语保留英文原词
- **标题层级**：`#` 只用于文档标题（由 Docusaurus 从 `title` front matter 渲染），正文从 `##` 开始，最深不超过 `####`
- **标题编号**：正文各级标题必须添加数字编号，便于阅读与引用
  - 一级正文标题：`## 1. 概述`、`## 2. 安装`
  - 二级正文标题：`### 1.1 背景`、`### 1.2 目标`
  - `index.md` 入口索引文件不强制编号
- **结构**：每篇文档需包含：概述 → 详细说明 → 示例（如适用）→ 注意事项
- **长度**：单文件建议不超过 500 行；内容过多时拆分子文件并在 index.md 中链接
- **代码块**：必须标注语言，如 ` ```js `、` ```bash `

---

## 文档导航规范

文档之间的跳转必须使用可点击的 Markdown 链接，禁止仅用代码块（反引号）展示路径。

Docusaurus 自动在每篇文档顶部生成面包屑导航，无需手动添加面包屑行。

### 链接写法

使用**绝对路径**（相对于 `baseUrl`，本仓库为站点根 `/`）：

```markdown
<!-- 正确：绝对路径，不带尾部斜杠 -->
[Cursor 使用指南](/docs/vibe-coding/cursor-guide)
[返回板块首页](/docs/vibe-coding/)

<!-- 错误：Liquid 语法（Jekyll 专属，已废弃） -->
[Cursor 使用指南]({{ '/docs/vibe-coding/cursor-guide/' | relative_url }})

<!-- 错误：相对路径 -->
[cursor-guide.md](./cursor-guide.md)
```

### 每篇文档必须包含的导航元素

| 文档类型 | 必须包含 |
|----------|---------|
| 首页（`src/pages/index.tsx`） | 进入文档的主操作（如 Hero CTA）、新手路线链接、各板块入口（卡片或表格） |
| 板块 `index.md` | `## 目录`（子文档链接表格，**置于正文最顶部**）、`## 目标`（读完后能做什么，列点说明） |
| 子文档（`.md`） | 无需手动面包屑（Docusaurus 自动生成） |

**板块 `index.md` 统一结构**：

```markdown
---
title: 板块名称
---

一句话介绍本板块的范围与价值。

## 目录

| 文件 | 内容 |
|------|------|
| [文档名](/docs/...) | 简短说明 |

## 目标

读完本板块，你应当能够：

- 能力点 1
- 能力点 2

## 其他章节（如有）

设计理念、核心原则等补充内容放在目标之后。
```

### 维护要求

- 新增文档时，**同步**更新：
  1. 父级 `index.md` 的表格中添加链接条目
  2. `sidebars.ts` 中添加对应条目
  3. `_category_.json` 如果需要调整顺序
- 新增板块时，同步更新：
  1. `src/pages/index.tsx` 首页中的相关入口（Hero / 路线 / 卡片等）
  2. `sidebars.ts` 新增 category
  3. `docusaurus.config.ts` navbar（如需要）
- 重命名或移动文件时，**同步**更新所有引用该文件的链接
- `_category_.json` 的 `position` 字段控制侧边栏顺序

---

## 样式与布局规范

- **主题色**：`#3d7c47`（primary）、`#7cb342`（dark mode primary），定义在 `src/css/custom.css` 的 Infima CSS 变量中
- **深色模式**：由 Docusaurus 内置支持，`data-theme="light"` / `data-theme="dark"` 属性由框架自动管理；用户选择自动持久化
- **侧栏折叠**：由 `docusaurus.config.ts` 中 `themeConfig.docs.sidebar.hideable: true` 控制；具体交互与布局以 `src/theme/DocRoot/Layout/` 下 swizzle 组件为准
- **样式修改**：优先在 `src/css/custom.css` 覆盖 Infima 变量；页面级或复杂组件使用 CSS Modules（`*.module.css`）。修改文档页整体布局时，谨慎升级/重新 swizzle，避免与上游主题冲突

---

## 可扩展性原则

- 新增板块时，在 `docs/` 下新建对应目录，并同步更新：
  1. `src/pages/index.tsx` 首页中的板块入口
  2. `sidebars.ts` 侧边栏
  3. `docusaurus.config.ts` navbar items（如需要）
  4. `README.md` 根目录文档目录表（板块状态一并更新）
- 不修改现有板块的目录名（避免破坏已有链接）
- 若建立 **`docs/knowledge/`** 等知识库板块，公共术语、缩写说明可统一维护在 `docs/knowledge/glossary.md`，并同步侧栏与首页

## 本地开发

```bash
npm install        # 安装依赖（首次或依赖变更后）
npm start          # 启动开发服务器，默认 http://localhost:3000/（baseUrl 为 /）
npm run build      # 生产构建，产物在 build/
npm run serve      # 本地预览构建产物
```

**注意**：`package.json` 中通过 `overrides` 将 `webpack` 固定在 `5.95.0`，原因是 `webpackbar@6` 与 webpack 5.96+ 存在 ProgressPlugin API 不兼容问题（Node 24 环境下复现）。升级 webpack 前须确认 `webpackbar` 已修复该问题。
